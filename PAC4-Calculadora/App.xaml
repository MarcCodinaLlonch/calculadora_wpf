<Application x:Class="PAC4_Calculadora.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PAC4_Calculadora"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!--colors de la calculadora-->
        <SolidColorBrush x:Key="ColorIgual"     Color="#ED4C4C"/>
        <SolidColorBrush x:Key="ColorControl"   Color="#FFAD73"/>
        <SolidColorBrush x:Key="ColorOperador"  Color="#3E3A39"/>
        <SolidColorBrush x:Key="ColorDigit"     Color="#B2B2B2"/>
        <SolidColorBrush x:Key="ColorPantalla"  Color="#D2E4E1"/>

        <!-- Estil animació botons amb efecte 3D (Degradats i Ombres) -->
        <Style x:Key="EstilBoto" TargetType="Button">
            <Setter Property="FontFamily" Value="pack://application:,,,/PAC4-Calculadora;component/Assets/Fonts/Botons/Inter/#Inter" />
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- El contenidor principal amb ombra paral·lela -->
                        <Border x:Name="BaseBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <Border.Effect>
                                <!-- Ombra inicial -->
                                <DropShadowEffect ShadowDepth="4" Direction="270" Color="Black" Opacity="0.4" BlurRadius="5"/>
                            </Border.Effect>
                            
                            <!-- Capa de degradat superposada per efecte volum -->
                            <Border x:Name="GradientOverlay" CornerRadius="5">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#70FFFFFF" Offset="0.0" />
                                        <GradientStop Color="#15FFFFFF" Offset="0.35" />
                                        <GradientStop Color="#10000000" Offset="0.65" />
                                        <GradientStop Color="#55000000" Offset="1.0" />
                                    </LinearGradientBrush>
                                </Border.Background>
                                
                                <ContentPresenter x:Name="Content"
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}">
                                    <ContentPresenter.RenderTransform>
                                        <TranslateTransform X="0" Y="0"/>
                                    </ContentPresenter.RenderTransform>
                                </ContentPresenter>
                            </Border>
                        </Border>
                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BaseBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <!-- Canvi ombra -->
                                <Setter TargetName="BaseBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="1" Direction="270" Color="Black" Opacity="0.4" BlurRadius="2"/>
                                    </Setter.Value>
                                </Setter>
                                <!-- Inverteix llum per simular "enfonsamnet" -->
                                <Setter TargetName="GradientOverlay" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#30000000" Offset="0.0" />
                                            <GradientStop Color="#10000000" Offset="0.2" />
                                            <GradientStop Color="#00FFFFFF" Offset="0.8" />
                                            <GradientStop Color="#20FFFFFF" Offset="1.0" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <!-- Mou el contingut de botó cap avall al clickar -->
                                <Setter TargetName="Content" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform X="0" Y="2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
